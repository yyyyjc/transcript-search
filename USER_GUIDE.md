# 媒體組使用指南 — 逐字稿語意檢索

> 給媒體組成員：5 分鐘上手

**🌐 工具網址**：https://yyyyjc.github.io/transcript-search/

---

## 這工具能幫你做什麼

**找出候選人歷年講過某主題的所有發言**。

例如想知道「候選人講過幾次變電所？什麼時候？什麼立場？」→ 輸入「變電所」→ 工具會掃過所有逐字稿，把含「變電所」的段落整理成卡片，每張卡含：
- 日期
- 場合（節目／受訪）
- 檔名
- 一句 AI 摘要
- 原文段落（關鍵字紅底高亮）
- Drive 連結（直接跳回原檔）

---

## 第一次使用

### 1. 開啟網址

https://yyyyjc.github.io/transcript-search/

### 2. 用 pumashen.org 帳號登入

點右上「使用 Google 登入」，選你的 @pumashen.org 信箱。

**第一次會跳同意畫面**，按「繼續」即可。**不會出現「未驗證應用程式」警告**（內部工具）。

### 3. 輸入主題、按「檢索」

例如「變電所」、「台電」、「社宅」、「長照」。

等個 10-30 秒，結果會出現。

---

## ⚠️ 這是 v1.0 「精確搜尋」 — 重要使用要點

工具目前是「字面精確比對」：你輸入什麼字，就找包含**那串字面**的段落。**它不會自動發散同義詞**。

### 實務影響

要找「能源相關的發言」？光搜「能源」可能漏掉只講「電力」「台電」「電網」的場合。

### 解法：拆成多個關鍵字、分別搜

```
搜「變電所」  → 拿到 2 筆
搜「台電」    → 拿到 3 筆（其中 1 筆跟變電所重複）
搜「電網」    → 拿到 1 筆
合計 5 筆獨立段落
```

**正在開發 v2.0「廣義搜尋」**——一次輸入主題、工具自動發散相關詞。完成前先用 v1.0 + 手動拆詞。

---

## 成本提醒

每次搜尋都要呼叫 Claude AI，會花錢。畫面右上會顯示：
- **本次查詢**：剛才那次花了多少（USD）
- **今日累計**：你今天總共花了多少

單次大約 **USD $0.02 - $0.10**（取決於命中段落數量）。月用量正常情況下會在 USD $5-15 之間。

請有成本意識，**不要連發搜尋**（連點按鈕在搜尋中會無效，這是設計上故意防呆）。

---

## 結果頁面操作

### 卡片功能

每張卡片右上有 **「在 Drive 開啟 ↗」** — 點了會新分頁打開原始逐字稿檔（可以看完整上下文）。

原文段落右側有 **「複製此句原文」** — 點了會把該段原文複製到剪貼簿，方便貼進文案。

### 批次操作

結果列右上有 **「下載 .csv」** — 把整批結果下載成 Excel/Google Sheets 可開的 CSV 檔，含 7 欄：date、category、occasion、filename、drive_link、summary、excerpt。

---

## 常見狀況

| 狀況 | 解釋 / 怎麼辦 |
|---|---|
| 搜尋按下沒反應 | 確認你已登入（右上有 email）；確認沒在搜尋中（按鈕是「搜尋中…」就等它） |
| 「沒有在 N 個檔案中找到…」 | 該關鍵字字面沒出現在任何檔。試別的相近詞 |
| 「你的帳號可能未在資料夾分享名單」 | 你的 email 沒被加進兩個 Drive 資料夾的分享名單；洽管理員 |
| 「分析服務暫時無回應」 | Worker 或 Claude API 短暫故障；等 1 分鐘重試 |
| 「請求太快，請稍等」 | 你 1 分鐘內請求超過 20 次（防範濫用機制）；等 1 分鐘 |
| 結果某筆「未知日期」 | 該檔名格式不符合解析規則；不影響找得到，只是日期欄空白 |
| 看到「N 個檔案無法讀取」 | 個別檔解析失敗（編碼異常等）；不阻擋其他結果 |

---

## 隱私須知

點頁尾「**隱私說明**」會跳出完整說明。重點：

- **你的 Google 帳號**：只用於讀 Drive 中指定資料夾，**絕不寫入、刪除、修改**任何內容
- **逐字稿內容**：搜尋時，命中段落會送 Anthropic Claude API 分析
- **Anthropic 政策**：企業 API 用戶請求預設**不用於模型訓練**
- **工具本身**：不保存任何逐字稿、查詢、結果

---

## 行動裝置

手機可用，排版會自動調整成單欄。但操作體驗以桌面為主。

---

## 出狀況找誰

工具維運者：（待填）

Bug 或建議都可以說。
